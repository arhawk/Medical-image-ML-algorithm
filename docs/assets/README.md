# Generated figures for docs/SHOWCASE.md
# Populated automatically when running medimg-train (full run or --quick).
# On Apple Silicon, run RF + MLP (--cpu-only) + CNN separately for baseline metrics.
